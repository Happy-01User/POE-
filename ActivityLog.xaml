<Window x:Class="lemo3.ActivityLog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lemo3"
        mc:Ignorable="d"
        Title="Activity Log"
        Height="650"
        Width="950"
        WindowStartupLocation="CenterScreen">

    <DockPanel Background="#F4F6F9">

        <!-- ================= TOP NAVIGATION ================= -->

        <Border DockPanel.Dock="Top"
                Height="65"
                Background="#1F3A5F">

            <Grid Margin="15,0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Activity Log"
                           Foreground="White"
                           FontSize="24"
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>

                <Button x:Name="btnChat"
                        Grid.Column="1"
                        Content="Chat"
                        Width="110"
                        Height="38"
                        Margin="8"
                        Click="btnChat_Click"/>

                <Button x:Name="btnTask"
                        Grid.Column="2"
                        Content="Tasks"
                        Width="110"
                        Height="38"
                        Margin="8"
                        Click="btnTask_Click"/>

                <Button x:Name="btnQuiz"
                        Grid.Column="3"
                        Content="Quiz"
                        Width="110"
                        Height="38"
                        Margin="8"
                        Click="btnQuiz_Click"/>

            </Grid>

        </Border>

        <!-- ================= CONTENT ================= -->

        <StackPanel Margin="25">

            <TextBlock Text="System Activity"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#1F3A5F"/>

            <TextBlock Text="View a history of quizzes completed, reminders created, and other recorded actions."
                       Margin="0,8,0,20"
                       FontSize="15"
                       Foreground="Gray"/>

            <Border BorderBrush="#D6D6D6"
                    BorderThickness="1"
                    CornerRadius="6"
                    Background="White"
                    Padding="10">

                <DataGrid x:Name="ActivityGrid"
                          AutoGenerateColumns="True"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          Height="470"/>

            </Border>

        </StackPanel>

    </DockPanel>

</Window>